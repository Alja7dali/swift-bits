// swift-tools-version:5.0
import PackageDescription

let package = Package(
  name: "Bits",
  products: [
    .library(name: "Bits", targets: ["Bits"])
  ],
  targets: [
    .target(name: "Bits", dependencies: [])
  ]
)